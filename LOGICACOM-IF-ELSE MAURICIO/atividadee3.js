var prompt = require('prompt-sync')()

var numeroSecreto = parseInt(Math.random()*101);
var tentativa = 1;
alert ("Seja Bem Vindo ao nosso jogo secreto!" );
alert ("Tente usar o menos numero de tentativa");
var escolha = prompt ("se desejar jogar sem limites, digite 'sem limites'")
if (escolha ="sem limites")
    while(chute !=)
